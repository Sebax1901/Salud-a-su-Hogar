<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marbey.saludasuhogar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_image"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaludASuHogar">
        <activity android:name=".view.ui.activities.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ui.activities.MainActivity" />
        <activity
            android:name=".view.ui.activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.SaludASuHogar.NoActionBar"/>
    </application>

</manifest>